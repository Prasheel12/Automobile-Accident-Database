CREATE TABLE crashes ( crashno INTEGER NOT NULL, location VARCHAR(50), crashdate DATE, crashtime VARCHAR(50), crashcause VARCHAR, crashcity VARCHAR(50), crashprovince VARCHAR(50), crashlat INT, crashlng INT, PRIMARY KEY (crashno) )